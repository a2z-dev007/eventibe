import WeddingVenuesClient from './WeddingVenuesClient';

export const metadata = {
  title: 'Wedding Venues | Eventibe',
  description: 'Browse and book premium wedding venues, banquet halls, and resorts.',
};

export default function WeddingVenuesPage() {
  return <WeddingVenuesClient />;
}
